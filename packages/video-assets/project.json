{
  "name": "video-assets",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/video-assets/src",
  "targets": {
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "pnpm exec rimraf dist/packages/video-assets",
          "pnpm exec rimraf packages/video-assets/dist"
        ]
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "remotion preview src/index.tsx",
        "cwd": "{projectRoot}"
      }
    },
    "build-storm-software": {
      "cache": true,
      "inputs": [
        "{projectRoot}/src/storm-software",
        "{projectRoot}/src/components",
        "{projectRoot}/src/types",
        "{projectRoot}/src/utilities",
        "{projectRoot}/src/style.css",
        "{projectRoot}/scripts"
      ],
      "outputs": ["{projectRoot}/dist/optimized/storm-software"],
      "executor": "nx:run-commands",
      "dependsOn": ["^build", "clean"],
      "options": {
        "command": "tsx scripts/build.ts storm-software",
        "cwd": "{projectRoot}"
      }
    },
    "build-cyclone-ui": {
      "cache": true,
      "inputs": [
        "{projectRoot}/src/cyclone-ui",
        "{projectRoot}/src/components",
        "{projectRoot}/src/types",
        "{projectRoot}/src/utilities",
        "{projectRoot}/src/style.css",
        "{projectRoot}/scripts"
      ],
      "outputs": ["{projectRoot}/dist/optimized/cyclone-ui"],
      "executor": "nx:run-commands",
      "dependsOn": ["^build", "clean"],
      "options": {
        "command": "tsx scripts/build.ts cyclone-ui",
        "cwd": "{projectRoot}"
      }
    },
    "build-powerlines": {
      "cache": true,
      "inputs": [
        "{projectRoot}/src/powerlines",
        "{projectRoot}/src/components",
        "{projectRoot}/src/types",
        "{projectRoot}/src/utilities",
        "{projectRoot}/src/style.css",
        "{projectRoot}/scripts"
      ],
      "outputs": ["{projectRoot}/dist/optimized/powerlines"],
      "executor": "nx:run-commands",
      "dependsOn": ["^build", "clean"],
      "options": {
        "command": "tsx scripts/build.ts powerlines",
        "cwd": "{projectRoot}"
      }
    },
    "build-acidic": {
      "cache": true,
      "inputs": [
        "{projectRoot}/src/acidic",
        "{projectRoot}/src/components",
        "{projectRoot}/src/types",
        "{projectRoot}/src/utilities",
        "{projectRoot}/src/style.css",
        "{projectRoot}/scripts"
      ],
      "outputs": ["{projectRoot}/dist/optimized/acidic"],
      "executor": "nx:run-commands",
      "dependsOn": ["^build", "clean"],
      "options": {
        "command": "tsx scripts/build.ts acidic",
        "cwd": "{projectRoot}"
      }
    },
    "build-shell-shock": {
      "cache": true,
      "inputs": [
        "{projectRoot}/src/shell-shock",
        "{projectRoot}/src/components",
        "{projectRoot}/src/types",
        "{projectRoot}/src/utilities",
        "{projectRoot}/src/style.css",
        "{projectRoot}/scripts"
      ],
      "outputs": ["{projectRoot}/dist/optimized/shell-shock"],
      "executor": "nx:run-commands",
      "dependsOn": ["^build", "clean"],
      "options": {
        "command": "tsx scripts/build.ts shell-shock",
        "cwd": "{projectRoot}"
      }
    },
    "build-earthquake": {
      "cache": true,
      "inputs": [
        "{projectRoot}/src/earthquake",
        "{projectRoot}/src/components",
        "{projectRoot}/src/types",
        "{projectRoot}/src/utilities",
        "{projectRoot}/src/style.css",
        "{projectRoot}/scripts"
      ],
      "outputs": ["{projectRoot}/dist/optimized/earthquake"],
      "executor": "nx:run-commands",
      "dependsOn": ["^build", "clean"],
      "options": {
        "command": "tsx scripts/build.ts earthquake",
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "cache": false,
      "inputs": ["{projectRoot}/dist"],
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "executor": "nx:run-commands",
      "dependsOn": [
        "clean",
        "typecheck",
        "build-storm-software",
        "build-cyclone-ui",
        "build-powerlines",
        "build-acidic",
        "build-shell-shock",
        "build-earthquake"
      ],
      "options": {
        "commands": [
          "pnpm copyfiles LICENSE dist/packages/video-assets",
          "pnpm copyfiles --up=2 ./packages/video-assets/*.md ./packages/video-assets/package.json dist/packages/video-assets",
          "pnpm copyfiles --up=4 \"./packages/video-assets/dist/optimized/*/media/*\" ./dist/packages/video-assets"
        ]
      }
    },
    "nx-release-publish": {
      "executor": "@storm-software/cloudflare-tools:r2-upload-publish",
      "options": {
        "bucketId": "storm-public",
        "tag": "media-kit",
        "path": "dist/packages/video-assets"
      }
    }
  },
  "tags": ["platform:node"]
}
